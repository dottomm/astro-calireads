---

import Detail from './Detail.astro';
import SGQVideo from '../data/sgqVideo.json';

---
      {SGQVideo.map(video => (
       <Detail open={video.open} dark={true} id={video.id} title={video.title}  url={video.url} src={video.src}  client:visible  />
      ))}


  